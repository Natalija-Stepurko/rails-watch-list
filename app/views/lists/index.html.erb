<h1>Your movie lists</h1>

<div>
  <% @lists.each do |list| %>
    <div class="movie-card" style="background-image: url('<%= list.image_url %>');">
      <h2><%= list.name  %></h2>
    </div>
  <% end %>
</div>

<%= link_to 'Create a movie list', new_list_path %>
